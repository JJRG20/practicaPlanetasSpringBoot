server:
  port: 3000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sistemaplanetas
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none   # usa tu BD existente
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: super_secret_key_123
  expiration: 7200000 # 2 horas
